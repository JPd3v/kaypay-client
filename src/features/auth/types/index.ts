interface LogedUser {
  alias: string;
  balance: number;
  email: string;
  firstName: string;
  id: number;
  lastName: string;
}

// eslint-disable-next-line import/prefer-default-export
export type { LogedUser };
